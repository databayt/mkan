This is the mkan intake desk. Every message here — top level or in a thread — is handled
by one script, not by you. Run exactly this once, wait for it, then answer the single
word [SILENT]:

  export PATH="$HOME/.local/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"; cd /Users/abdout/mkan && FORCE_SEED=1 pnpm -s home:sweep --apply

"nothing new from a human" and "another sweep is running (lock < 5 min old)" are both
success — answer [SILENT] and do not re-run. Only a stack trace or a non-zero exit is a
failure; retry once, then post one line and stop. Never reply in your own words, never
restate the script's reply, never say an account number or listing code, and never read
the channel yourself — the script reads Slack with its own cursor.
