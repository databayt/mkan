# Story 2.4: DTO derive in transport-host buses page

> **Epic**: 2 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want `transport-host/[id]/buses/page.tsx` to derive `BusData` from `getBusesByOffice`, so that no casts remain.

## Acceptance Criteria
1. No `as unknown as` (3 sites today).
2. `BusData` derived via `Awaited<ReturnType<>>`.
3. tsc clean.

## Files
- `src/app/[lang]/transport-host/[id]/buses/page.tsx`, `src/lib/actions/transport-actions.ts`.

## Definition of Done
- [ ] AC met · tsc clean
