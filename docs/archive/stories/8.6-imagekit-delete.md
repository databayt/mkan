# Story 8.6: ImageKit delete on upload route

> **Epic**: 8 · **Status**: To Do · **Phase**: C

## Story
As a developer, I want DELETE on `/api/upload` to actually remove the asset from ImageKit, so that we don't accumulate orphan files.

## Acceptance Criteria
1. `/api/upload/route.ts` DELETE handler calls `imagekit.deleteFile(fileId)` (replaces TODO line 143).
2. Returns 204 on success, error envelope on failure.

## Files
- `src/app/api/upload/route.ts`, `src/lib/imagekit.ts`.

## Definition of Done
- [ ] AC met · TODO removed
