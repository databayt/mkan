# Story 2.6: ESLint rule banning `as unknown as`

> **Epic**: 2 · **Status**: To Do · **Phase**: B

## Story
As a maintainer, I want ESLint to block new `as unknown as` introductions in `src/app/**`, so that future drift can't reintroduce the smell.

## Acceptance Criteria
1. `eslint.config.mjs` has a rule (custom or via `no-restricted-syntax`) that errors on `TSAsExpression > TSUnknownKeyword` in `src/app/**`.
2. Existing files (post-Stories 2.2-2.5) are clean.
3. CI lint:strict green.

## Tasks
- [ ] Use `no-restricted-syntax` with selector `TSAsExpression[typeAnnotation.type='TSUnknownKeyword']`.
- [ ] Scope rule to `src/app/**` via overrides.

## Files
- `eslint.config.mjs`.

## Definition of Done
- [ ] AC met · lint:strict exit 0
