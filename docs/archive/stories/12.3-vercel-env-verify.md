# Story 12.3: Verify Vercel env vars

> **Epic**: 12 · **Status**: To Do · **Phase**: F

## Story
As a release manager, I want every required env var (per `src/lib/env.ts`) set in Vercel for production + preview, so that the deploy doesn't fail at runtime.

## Acceptance Criteria
1. All keys from `src/lib/env.ts` schema present on Vercel for the production environment.
2. Stripe **live** keys (not test) on production env.
3. `STRIPE_WEBHOOK_SECRET` matches the live webhook endpoint.
4. `RESEND_API_KEY` present.
5. ImageKit / Upstash / Neon / OAuth all present.

## Tasks
- [ ] Open Vercel project → Settings → Env vars; check each.
- [ ] If any missing, add and redeploy preview to verify.

## Files
- (Vercel dashboard; no repo changes).

## Definition of Done
- [ ] AC met · checklist saved as comment
