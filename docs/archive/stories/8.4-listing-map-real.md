# Story 8.4: Real listing-detail map

> **Epic**: 8 · **Status**: To Do · **Phase**: C

## Story
As a guest, I want a real map on the listing detail page (not "Map Coming Soon"), so that I see where the property is.

## Acceptance Criteria
1. Mapbox map at the listing's lat/lng (or fallback Leaflet).
2. Single canonical component at `src/components/listings/property/detail.tsx` (rename from `detial.tsx`).
3. Duplicate at `src/components/site/property/detial.tsx` removed.
4. Replaces "Map Coming Soon" placeholder.

## Files
- both files; merge into one.

## Definition of Done
- [ ] AC met · single component · no placeholder
