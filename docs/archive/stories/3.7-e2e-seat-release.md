# Story 3.7: E2E seat-release sweeper

> **Epic**: 3 · **Status**: To Do · **Phase**: E

## Story
As QA, I want an E2E covering the seat-release sweeper (`releaseExpiredSeatHolds` cron), so that held-but-unpaid seats are reliably freed after 30 minutes.

## Acceptance Criteria
1. `tests/transport/seat-release.spec.ts` creates a booking with seat hold, sets `holdExpiresAt` to past.
2. Hits `/api/cron/release-seats` with the cron secret.
3. Verifies seat status flipped from `Reserved` to `Available` and booking marked `EXPIRED`.

## Files
- new `tests/transport/seat-release.spec.ts`.

## Definition of Done
- [ ] AC met · spec green
