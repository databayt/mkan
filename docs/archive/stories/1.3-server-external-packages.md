# Story 1.3: serverExternalPackages in next.config.ts

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want known ESM-only server libs externalized in Next 16, so that we don't hit `require() of ES Module` runtime errors on Vercel.

## Acceptance Criteria
1. `next.config.ts` declares `serverExternalPackages` with at least: `['@react-pdf/renderer']`. Add others if any sentry/jsdom/opentelemetry transitives reappear.
2. `pnpm build` succeeds.
3. Production runtime (post-merge to main) does not error on import.

## Tasks
- [ ] Add `serverExternalPackages` array to `next.config.ts`.
- [ ] Run `pnpm build` to verify.

## Dev Notes
- Files: `next.config.ts`.
- Memory note: jsdom/sentry already removed; this is preventative.

## Tests
- Unit: n/a.
- E2E: build job in CI exercises this.

## Definition of Done
- [ ] AC met · build green
