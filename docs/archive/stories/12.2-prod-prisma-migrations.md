# Story 12.2: Apply Prisma migrations to prod

> **Epic**: 12 · **Status**: To Do · **Phase**: F

## Story
As a release manager, I want pending Prisma migrations applied to the production Neon DB, so that the prod schema matches `ship/v1.0`.

## Acceptance Criteria
1. Wake Neon prod DB.
2. `pnpm prisma migrate deploy` against prod URL.
3. Verify schema versions match.
4. Backup taken before migration (Neon point-in-time recovery).

## Tasks
- [ ] Set `DATABASE_URL` to prod (one-shot env).
- [ ] Run `prisma migrate deploy`.
- [ ] Verify with `prisma migrate status`.

## Files
- `prisma/migrations/*` (committed).

## Definition of Done
- [ ] AC met · prod schema in sync
