# Story 4.13: E2E Stripe sandbox happy path

> **Epic**: 4 · **Status**: To Do · **Phase**: C

## Story
As QA, I want an E2E spec covering the full Stripe payment flow against the test mode, so that we catch regressions in the integration.

## Acceptance Criteria
1. `tests/e2e/homes-stripe-payment.spec.ts` creates a booking, picks Stripe, fills test card 4242, completes payment.
2. Simulates webhook delivery (signed event POST).
3. Verifies booking status `CONFIRMED`, payment `PAID`, ticket/email path triggered.
4. Tagged `@stripe` (separate from `@smoke` since it relies on test secrets).

## Files
- new `tests/e2e/homes-stripe-payment.spec.ts`.

## Definition of Done
- [ ] AC met · spec green in CI (with test keys)
