# Story 1.1: Bump CI Node to 22

> **Epic**: 1 — Build & Deploy Hardening · **Status**: To Do · **Phase**: B

## Story
As a release engineer, I want CI to run on Node 22 (matching `package.json` engines and Vercel's runtime), so that CI green is a real signal that prod will build.

## Acceptance Criteria
1. `.github/workflows/ci.yml` uses `NODE_VERSION: '22'` (or `22.12.0`).
2. Every job (`typecheck`, `lint`, `test`, `build`) runs on Node 22.
3. CI is green on this branch immediately after the change.

## Tasks
- [ ] Update `NODE_VERSION` env in `ci.yml`.
- [ ] Confirm `actions/setup-node@v4` reads the env var.
- [ ] Push branch and verify CI green.

## Dev Notes
- Files: `.github/workflows/ci.yml`.
- Reuse: existing matrix.
- Version pin: match `engines.node` exactly.

## Tests
- Unit: n/a.
- E2E: n/a.
- Verification: CI run goes green.

## Definition of Done
- [ ] AC met
- [ ] CI green
- [ ] Reviewed
