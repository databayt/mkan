# Story 12.1: Full test matrix green

> **Epic**: 12 · **Status**: To Do · **Phase**: F

## Story
As a release manager, I want the full test matrix (unit + E2E + lint:strict + tsc) to be green on `ship/v1.0` immediately before merge, so that we can ship with confidence.

## Acceptance Criteria
1. `pnpm tsc --noEmit` exit 0.
2. `pnpm lint:strict` exit 0.
3. `pnpm test --reporter=verbose` exit 0, ≥900 unit tests.
4. `pnpm test:e2e` exit 0, ≥30 E2E specs.
5. `pnpm build` exit 0.

## Tasks
- [ ] Run all 5 commands locally.
- [ ] If any fail, file blocker stories and fix.

## Files
- (verification only; fix files as needed).

## Definition of Done
- [ ] All 5 commands green
