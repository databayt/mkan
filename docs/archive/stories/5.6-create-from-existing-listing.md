# Story 5.6: Create from existing listing

> **Epic**: 5 · **Status**: To Do · **Phase**: C

## Story
As a host with multiple similar properties, I want to clone an existing listing as a starting point, so that I don't re-enter every field.

## Acceptance Criteria
1. `/host/page.tsx` "Create from existing listing" action implemented (replaces TODO `host/content.tsx:49`).
2. `duplicateListing(listingId)` server action: ownership check, deep copy of fields, null `id`, `publishedAt`, `bookings`, `reviews`; status `DRAFT`.
3. Redirects to `/host/[newId]/about-place`.

## Files
- `src/app/[lang]/host/content.tsx`, `src/lib/actions/listing-actions.ts` (extend).

## Tests
- Unit: duplicate clears the right fields.

## Definition of Done
- [ ] AC met · TODO removed · tests green
