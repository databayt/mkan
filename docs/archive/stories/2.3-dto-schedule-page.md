# Story 2.3: DTO derive in transport-host schedule page

> **Epic**: 2 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want `transport-host/[id]/schedule/page.tsx` to derive `RouteData`/`BusData`/`TripData` types from their action returns, so that no `as unknown as` casts remain.

## Acceptance Criteria
1. No `as unknown as` in the file (4 sites today).
2. Three DTOs derived from `getRoutesByOffice`, `getBusesByOffice`, `getTripsByOffice`.
3. tsc clean.

## Tasks
- [ ] Apply DTO pattern (see Story 2.1).
- [ ] Map Decimals if needed.

## Files
- `src/app/[lang]/transport-host/[id]/schedule/page.tsx`, `src/lib/actions/transport-actions.ts`.

## Definition of Done
- [ ] AC met · tsc clean · tests green
