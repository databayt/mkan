# Story 10.1: Refactor processPayment to dispatch by gateway

> **Epic**: 10 · **Status**: To Do · **Phase**: D

## Story
As a developer, I want `processPayment` in transport-actions.ts to dispatch to a gateway-specific handler, so that adding gateways is a one-handler addition.

## Acceptance Criteria
1. `processPayment({ bookingId, gateway, ... })` switches on gateway and calls handler.
2. Handlers: `processStripe`, `processBankak`, `processCashi`, `processMobileMoney`, `processBankTransfer`, `processCash`.
3. Uses unified `Payment` model from Story 4.1.
4. Unit tests for each handler.

## Files
- `src/lib/actions/transport-actions.ts`.

## Definition of Done
- [ ] AC met · tests green
