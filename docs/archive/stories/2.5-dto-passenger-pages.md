# Story 2.5: DTO derive in passenger pages

> **Epic**: 2 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want passenger-side pages to derive types from their actions, so that no casts remain.

## Acceptance Criteria
1. No `as unknown as` in:
   - `src/app/[lang]/transport/trips/[id]/page.tsx` (2 sites)
   - `src/app/[lang]/transport/booking/checkout/content.tsx` (1)
   - `src/app/[lang]/transport/booking/[id]/page.tsx` (1)
   - `src/app/[lang]/transport/booking/[id]/ticket/page.tsx` (1)
2. Types derived from `getTripDetails`, `getBooking`, `generateTicketData`.
3. tsc clean.

## Files
- 4 files above + `src/lib/actions/transport-actions.ts` (action return mapping if needed).

## Definition of Done
- [ ] AC met · tsc clean · existing E2E green
