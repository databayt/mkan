# Story 1.8: .env.example cleanup

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As an operator provisioning a new env, I want `.env.example` to list exactly the keys the app needs (no stale, no missing), so that I can copy-paste with confidence.

## Acceptance Criteria
1. Stale `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` removed (Sentry was removed).
2. New keys for Stripe (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`) added.
3. Each key has an inline comment explaining purpose + dev/prod requirement.
4. Order matches Story 1.2's env schema for easy comparison.

## Tasks
- [ ] Edit `.env.example`.
- [ ] Cross-check with `src/lib/env.ts` (Story 1.2).

## Dev Notes
- Files: `.env.example`.

## Tests
- Verification: `pnpm dev` boots cleanly with vars from the example.

## Definition of Done
- [ ] AC met · matches env schema
