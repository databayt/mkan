# Story 4.2: createStripePaymentIntent

> **Epic**: 4 · **Status**: To Do · **Phase**: C

## Story
As a guest, I want to pay for a booking via Stripe, so that I can use an international card.

## Acceptance Criteria
1. `createStripePaymentIntent({ bookingId })` action returns `{ ok: true, clientSecret }` for the authenticated owner.
2. Action validates booking ownership, total amount, currency.
3. Stripe `paymentIntent` created with `metadata.kind = "booking"`, `metadata.bookingId = ...`.
4. `Payment` row created with `gateway = STRIPE`, `gatewayPaymentId = pi_...`, `status = PENDING`.
5. Replaces TODO at `src/lib/actions/payment-actions.ts:637`.

## Files
- `src/lib/actions/payment-actions.ts`, `src/lib/env.ts` (uses `STRIPE_SECRET_KEY`).

## Tests
- Unit: `tests/actions/payment-actions.test.ts` — mock Stripe SDK.

## Definition of Done
- [ ] AC met · tsc clean · unit test green
