# Story 3.8: Test factory helpers

> **Epic**: 3 · **Status**: To Do · **Phase**: B

## Story
As a test author, I want typed factory helpers (`makeUser`, `makeListing`, `makeTransportOffice`, `makeTrip`, `makeBooking`), so that specs DRY their setup.

## Acceptance Criteria
1. `tests/helpers/factories.ts` exports the above functions.
2. Each accepts a partial override and inserts into DB.
3. ≥3 specs use them.
4. Use Prisma directly (not actions) to bypass auth gates.

## Tasks
- [ ] Create `tests/helpers/factories.ts`.
- [ ] Provide sane defaults (Faker for names, hashed passwords, etc.).
- [ ] Refactor 3 existing specs to use them.

## Files
- new `tests/helpers/factories.ts`, modified specs.

## Definition of Done
- [ ] AC met · specs still green
