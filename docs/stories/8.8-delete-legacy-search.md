# Story 8.8: Delete legacy redirected pages

> **Epic**: 8 · **Status**: To Do · **Phase**: C

## Story
As a developer, I want legacy redirected pages deleted (the redirects in next.config handle the URLs), so that we don't carry dead code.

## Acceptance Criteria
1. Delete `src/app/[lang]/search/page.tsx`, `src/app/[lang]/(nondashboard)/searching/page.tsx`, `src/app/[lang]/(nondashboard)/searching/[id]/page.tsx`.
2. Confirm redirects in `next.config.ts` still cover the URLs.
3. E2E `tests/e2e/search-consolidation.spec.ts` still passes.

## Files
- delete 3 files.

## Definition of Done
- [ ] AC met · search-consolidation E2E green
