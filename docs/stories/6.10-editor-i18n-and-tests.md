# Story 6.10: Editor i18n keys + unit tests

> **Epic**: 6 · **Status**: To Do · **Phase**: C

## Story
As a developer, I want every editor page's strings keyed in dictionaries (en + ar) and covered by a smoke unit test, so that we maintain i18n parity and catch regressions.

## Acceptance Criteria
1. New keys under `editor.*` in `en.json` and `ar.json` for all 9 pages.
2. Parity test in `tests/components/i18n/` ensures `editor.*` matches between en/ar.
3. Unit test per page renders without crash.

## Files
- `src/components/internationalization/{en,ar}.json`, `tests/components/i18n/parity.test.ts`.

## Definition of Done
- [ ] AC met · parity test green
