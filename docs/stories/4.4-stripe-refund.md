# Story 4.4: Stripe refund

> **Epic**: 4 · **Status**: To Do · **Phase**: C

## Story
As an admin (or via cancellation policy), I want to refund a Stripe payment, so that guests get money back per policy.

## Acceptance Criteria
1. `processRefund({ paymentId, amount? })` action calls Stripe's refund API.
2. Updates `Payment.refundedAt`, `Payment.refundedAmount`, `Payment.status = REFUNDED`.
3. Sends refund email to guest.
4. Replaces TODO at `payment-actions.ts:677`.

## Files
- `src/lib/actions/payment-actions.ts`, `src/lib/mail-templates/refund.tsx`.

## Tests
- Unit: mock Stripe SDK; test partial vs full refund.

## Definition of Done
- [ ] AC met · tests green
