# Story 1.9: Remove stray files & gitignore tsbuildinfo

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want a clean repo root (no orphan files, no committed build artifacts), so that the working tree is a faithful snapshot.

## Acceptance Criteria
1. `Drepomkanprismamigrationsadd_performance_indexes.sql` (1 byte) deleted.
2. `Drepomkansrcappapihealthroute.ts` (1 byte) deleted.
3. `tsconfig.tsbuildinfo` removed from version control and added to `.gitignore`.
4. `git status` shows no untracked artifacts after fresh `pnpm tsc --noEmit`.

## Tasks
- [ ] `git rm` the 1-byte stray files.
- [ ] Add `tsconfig.tsbuildinfo` and `*.tsbuildinfo` to `.gitignore`.
- [ ] `git rm --cached tsconfig.tsbuildinfo`.

## Dev Notes
- Files: `.gitignore`, repo root.

## Tests
- Verification: `git status` clean after a fresh build.

## Definition of Done
- [ ] AC met · clean working tree
