# Story 2.2: DTO derive in transport-host routes page

> **Epic**: 2 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want `transport-host/[id]/routes/page.tsx` to derive its `RouteData` type from `getRoutesByOffice` instead of casting, so that any action shape change surfaces as a type error.

## Acceptance Criteria
1. No `as unknown as RouteData[]` in the file (3 sites today).
2. `RouteData` is `Awaited<ReturnType<typeof getRoutesByOffice>>` (extracted via conditional type).
3. Action returns Decimal-mapped numbers (price, duration), not Prisma `Decimal`.
4. tsc clean.

## Tasks
- [ ] Read `getRoutesByOffice` return shape.
- [ ] If Decimals exist, map them via `.toNumber()` inside the action.
- [ ] In the page, import action and derive type.
- [ ] Remove all `as unknown as` casts.

## Dev Notes
- Files: `src/app/[lang]/transport-host/[id]/routes/page.tsx`, `src/lib/actions/transport-actions.ts`.

## Tests
- Unit: extend `tests/actions/transport-actions.test.ts` if Decimal mapping changes.
- E2E: existing `tests/transport/host-onboarding.spec.ts` still passes.

## Definition of Done
- [ ] AC met · tsc clean · existing tests green
