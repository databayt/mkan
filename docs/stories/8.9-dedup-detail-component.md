# Story 8.9: Dedup property detail component

> **Epic**: 8 · **Status**: To Do · **Phase**: C

## Story
As a developer, I want a single canonical property-detail component (not two misspelled `detial.tsx`), so that we maintain one source of truth.

## Acceptance Criteria
1. Rename `src/components/listings/property/detial.tsx` → `detail.tsx`.
2. Delete `src/components/site/property/detial.tsx` (move logic into the canonical one if it differs).
3. Update all imports.

## Files
- rename + delete + import updates.

## Definition of Done
- [ ] AC met · build clean
