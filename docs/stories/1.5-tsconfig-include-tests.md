# Story 1.5: tsconfig includes tests/

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want test code typechecked alongside source, so that test files don't silently rot.

## Acceptance Criteria
1. `tsconfig.json` no longer excludes `tests/`.
2. `pnpm tsc --noEmit` passes including test files.
3. If any type errors surface in test code, fix them.

## Tasks
- [ ] Remove `"tests"` from `exclude` in `tsconfig.json`.
- [ ] Run `pnpm tsc --noEmit`; fix any errors.
- [ ] Update `vitest.config.ts` if needed (it has its own typecheck mode).

## Dev Notes
- Files: `tsconfig.json`, possibly test files.
- Tests are 38 files; surface area limited.

## Tests
- Verification: tsc green.

## Definition of Done
- [ ] AC met · tsc green
