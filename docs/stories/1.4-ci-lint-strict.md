# Story 1.4: lint:strict in CI

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want CI to fail on any ESLint warning, so that warnings don't accumulate and rot.

## Acceptance Criteria
1. `lint` job in `ci.yml` runs `pnpm lint:strict` (which is `eslint . --max-warnings 0`).
2. CI is green on this branch.
3. If warnings exist, fix them as part of this story.

## Tasks
- [ ] Update `ci.yml` lint step.
- [ ] Run `pnpm lint:strict` locally; fix any warnings.
- [ ] Push and verify.

## Dev Notes
- Files: `.github/workflows/ci.yml`, possibly source files (warning fixes).
- Reuse: `lint:strict` script already in `package.json`.

## Tests
- Unit: n/a.
- E2E: n/a.

## Definition of Done
- [ ] AC met · lint:strict exit 0 · CI green
