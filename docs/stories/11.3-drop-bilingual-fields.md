# Story 11.3: Drop nameAr/descriptionAr DB fields

> **Epic**: 11 · **Status**: To Do · **Phase**: D

## Story
As a developer, I want a single source of truth for translations (dictionaries), so that we don't carry bilingual DB columns that conflict with the i18n rule.

## Acceptance Criteria
1. Migration drops `nameAr`, `descriptionAr` from `TransportOffice` and `AssemblyPoint`.
2. Existing data migrated to dictionary keys (one-time backfill script if needed).
3. UI reads names from dictionaries via slug lookup.

## Files
- `prisma/schema.prisma`, migration, components reading these fields.

## Definition of Done
- [ ] AC met · build green
