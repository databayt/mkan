# Story 12.5: Post-deploy smoke

> **Epic**: 12 · **Status**: To Do · **Phase**: F

## Story
As a release manager, I want a manual smoke against the production URL after deploy, so that we catch any obvious prod-only break.

## Acceptance Criteria
1. `/api/health` returns 200 with valid JSON.
2. `/`, `/en/`, `/ar/`, `/en/listings`, `/en/transport`, `/ar/listings`, `/ar/transport` all 200.
3. Login flow works (test account).
4. Search returns results.
5. LCP under 2.5s on each (eyeball via Chrome DevTools).

## Tasks
- [ ] Hit each URL.
- [ ] Capture screenshots.
- [ ] Note any anomalies.

## Files
- (manual; produce checklist comment).

## Definition of Done
- [ ] AC met · checklist passed
