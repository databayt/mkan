# Story 1.7: Prettier config + format pass

> **Epic**: 1 · **Status**: To Do · **Phase**: B

## Story
As a developer, I want consistent formatting across the codebase, so that PR diffs reflect intent not whitespace.

## Acceptance Criteria
1. `.prettierrc` at repo root with project conventions (tabs/spaces, semi, single-quote, trailing comma, print width).
2. `.prettierignore` covers `node_modules`, `.next`, `tsconfig.tsbuildinfo`, generated files.
3. `pnpm exec prettier --write .` runs once to format all files.
4. `pnpm exec prettier --check .` exit 0 thereafter.

## Tasks
- [ ] Create `.prettierrc` (`{ "semi": true, "singleQuote": false, "trailingComma": "all", "printWidth": 100 }` or match existing style).
- [ ] Create `.prettierignore`.
- [ ] Run `prettier --write .` and commit the format pass as a separate commit.

## Dev Notes
- Files: new `.prettierrc`, `.prettierignore`; many touched by formatter.
- Format-only commit MUST be its own PR/commit so review is trivial.

## Tests
- Verification: `prettier --check .` green.

## Definition of Done
- [ ] AC met · format pass committed separately
