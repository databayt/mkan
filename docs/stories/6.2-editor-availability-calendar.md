# Story 6.2: Editor — availability calendar

> **Epic**: 6 · **Status**: To Do · **Phase**: C

## Story
As a host, I want a full month-grid calendar in the editor to block dates and view existing bookings, so that I control my availability.

## Acceptance Criteria
1. `/hosting/listings/editor/[id]/details/availability/page.tsx` shows month grid.
2. Past dates disabled.
3. Booked dates colored + non-clickable.
4. Click a future date → toggle blocked → calls `setBlockedDate({ listingId, date })`.
5. Removes the inline `"Calendar view coming soon"` placeholder.

## Files
- that page, reuses `src/components/hosting/calendar-grid.tsx` from Story 5.2.

## Tests
- Component: calendar renders, click toggles blocked state.

## Definition of Done
- [ ] AC met · placeholder removed
