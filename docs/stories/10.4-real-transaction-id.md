# Story 10.4: Real transactionId

> **Epic**: 10 · **Status**: To Do · **Phase**: D

## Story
As a developer, I want `Payment.transactionId` to hold the real provider ID (not a placeholder), so that we can reconcile and refund correctly.

## Acceptance Criteria
1. Stripe path: `transactionId = paymentIntent.id`.
2. Reference-based: `transactionId = PaymentReference.reference`.
3. Cash: `transactionId = null`.
4. No fake string assignments remain.

## Files
- `src/lib/actions/transport-actions.ts` (and homes payment-actions if same pattern).

## Definition of Done
- [ ] AC met · grep confirms no placeholder
